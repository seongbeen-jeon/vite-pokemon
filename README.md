개발 규칙
1. 컴포넌트는 모두 객체를 매개 변수로 받는다.
2. 함수는 매개변수가 하나 일 때는 변수로, 2개 이상일 때 객채로 받아 구조분해 할당을 사용한다.

수정할 사항 // 간단한 문제, 파악된 문제, 기술적 이슈 X
    기능
        - 검색바 사용 검색시 "정확히 일치" 기능 추가하기
            ex: "파이어" 검색시 "파이어", "파이어로", "캠프 파이어" 모두 검색된다.
    뷰
        - Pages / Card Detail 디자인하기
        - data를 받아오기전에 랜더링 시 undefined를 읽어와서 페이지가 로딩 되지않아 loading 글자만 뜨다가 로딩 완료된 이후 뷰가 그려진다. 로딩중에도 틀이 보이게 끔 뷰를 바꾸기.

    DB
        - S6K 박스의 카드 이미지가 DB에 존재하지 않는다.

앞으로 추가할 사항
    기능
        - 위시 리스트 -> 갖고 싶은 카드들을 선택해서 앨범에서 미리 볼 수 있게 한다. 같이 모아두면 보기 좋은 카드들을 미리 확인.위시리스트 카드는 반투명하게 해서 구별
        - 그룹 서비스 -> 다른 사람과 같은 그룹을 맺어 내 앨범 뿐만아니라 다른 사람의 앨범을 볼 수 있도록 그룹 서비스 구현
        - 엑셀 시트에서 하던거 옮겨 오기.
        - ebay API 이용해서 가격 정보 불러오기
        - PSA 등급 카드를 불러오기 -> 싱글 카드와 어떤식으로 분리할지 고민하자. ex 앨범 , 등급카드 앨범 or  앨범 -> 필터링 등급카드
    뷰

트러블 이슈
    2026-09-23
        1. 카드 디테일 페이지 로딩이 되지 않는 오류 발생

            1.1. 원인 : 해외판 이름을 불러오기 위한 기능을 추가하였으나 도감 번호로 join하였기 때문에 도감 번호가 없는 non-pokemon type은 card.pokemon 내부에 값이 없어 react에서 읽어 올 수 없었기에 페이지가 로딩되지 않았다.

            1.2. 해결방안 :
                1.2.1. 1번안 : DB 구조를 변경해서 현재 pokemon table에서 관리하는 해외판 이름을 각 card 마다 붙여줘서 관리
                    -> 문제 : 모든 카드의 해외판 이름에 대한 data를 구해야한다.
                1.2.2. 2번안 : non-pokemon type의 디테일 페이지 로딩시 card.pokemon에 값이 없을시 해당 부분 랜더링을 무시한다.
                    -> 문제 : 근본적인 문제를 해결 할 수 없다. 해외판 이름을 포켓몬 도감에서 포켓몬들의 기본형 이름만 관리하는것이 문제다.
                
                선택 2번안 : 일단 페이지 랜더링 이슈를 먼저 해결하고 차후에 1번안으로 바꿔야겠다.
    
챌린지
    1. vercel을 사용해서 배포중인데 404가 너무 자주 뜬다. 문제 확인 필요

    2. BoxDetail 페이지 최적화 :  박스 디테일에서 로딩시 100~300장 정도의 이미지를 받아오는데 너무 오래 걸린다. 해결 필요
        2.1. 초기지연 개선 - lazy loading, 무한 스크롤 도입 검토
        2.2. 페이지 이동 시 캐싱을 통해 딜레이 개선
            2.2.1 브라우저 캐시
                -R2/CDN의 Cache-Control 설정 확인
                -이미지 URL이 동일할때 브라우저 캐시가 재사용 되는지 확인
                -캐시 적중 및 재다운로드 여부를 DevTools에서 측정
            2.2.2. 클라이언트 데이터 캐시
                -React Query(TanStack Query) 도입 검토
                -카드 목록에 대한 QueryKey 설계
                -staleTime, gcTime 등을 활용한 캐시 수명관리
            분석할 사항
                - 동일 페이지 재방문 시 API 요청 수
                - 이미지 요청 수 및 전송량
                - 캐시 적중 여부
                - 페이지 재방문 후 화면 표시 시간
                - 캐시 데이터의 최신화및 갱신 동작
    
    3. 필터와 정렬이 반복될 때의 성능및 상태 관리 : 필터와 정렬이 섞여서 사용될 때 정렬이 깨지거나 딜레이가 생기는 경우 발생
        3.1. 원본데이터와 추가로 생성된 데이터 등에 중복이 발생하는지 확인 필요
        3.2. 불필요한 렌더링이 추가로 발생하는지 확인 필요
    
    4. 검색 기능의 확장과 네트워크 요청 최적화
        4.1. 검색 결과 상태분리
            4.1.1. 로딩, 빈결과, 오류, 이전 결과 표시 정책 분리
        4.2. Debounce가 필요한 상황인지 확인필요
    
    5. 앨범의 대량 등록및 수정 처리
        5.1. 여러 카드를 한번에 등록할 때 UX를 상세하게 검토
            5.1.1. 코드 검색을 통한 빠른 입력이 주요하지만 / 로 분리하는것이 사용자 입장에서 편리한지, 다른 더 편한 방법이 있는지 검토
        5.2. 수량과 언어를 수정할 때마다 DB 요청이 발생
            5.2.1. 여러 업데이트 내용을 한번의 요청으로 처리할지 검토
            5.2.2. 한번에 처리 한다면 중간에 서버가 닫히거나 문제가 생겼을때 해결방안 검토
        5.3. 중복 카드 처리
            5.3.1. 언어에 따라 같은 id의 카드가 여러장 존재할 수 있는데 이런경우 insert 페이지에서 추가할 때 이미 존재하는 카드가 반려당하는경우 해결하기
            5.3.2. card detail 페이지에서 간편 등록시 같은 언어 임에도 따로 수량이 집계된다. 같은 언어라면 수량을 늘리는 방향으로 가고, 다른 언어라면 어떤식으로 분리 할지 방법 검토
        5.4. 실패 처리 - 현재 실패시 단순하게 실패 알림만 발생 여러 상황에 따라 분리하여 관리
            5.4.1. 일부 데이터 삽입 실패시 화면 상태 및 상황을 관리하는 방법 검토 - 성공한 항목과 실패한항목을 사용자에게 표시
            5.4.2. 재시도 가능한 오류가 재시도 불가능한 오류 구분
    6. 카드 이미지 저장 및 파이프라인 최적화
        6.1. 현재 데스크탑 로컬에 받아서 저장, 가공 이후 R2 및 supabase에 등록
            6.1.1. Backend를 구현해서 웹에서 한번에 처리할 수 있는 파이프라인 구현
        6.2. Webp 품질 설정및 파일 크기와 시각적 품질 비교
            6.2.1. 카드 표시 크기에 맞춘 이미지 해상도 최적화 필요. 작은 카드는 낮은 품질, 크게 봐야할 때는 고품질 해상도가 필요
            6.2.2. R2/CDN 캐시 헤더 설정 확인
        6.3. 파일 이름 및 경로 규칙을 만들어 이미지 관리의 최적화 필요
